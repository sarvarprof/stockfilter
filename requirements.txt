# Core
Flask>=3.0,<4.0
gunicorn>=21.0
yfinance>=0.2.40
requests>=2.31

# Sentiment analysis (FinBERT) — optional, ~2 GB with torch
transformers>=4.40
torch>=2.2

# Claude-backed sentiment (optional; requires ANTHROPIC_API_KEY env var)
anthropic>=0.34

# HTML parsing for insider scraper
beautifulsoup4>=4.12

# Pandas is pulled in by yfinance but pin for safety
pandas>=2.0
